# War Intelligence Domain

Current WIH product boundary: rendered-page observation with browser-local history; no Torn API background collection is authorized.

Discovery already live-proved public `GET /faction/{id}/members` can provide structured faction member status/last-action data even with `access.faction:false`; `/key/info` selection enumeration was not a universal manifest for that Public capability.

Still open: API freshness, hospital-state shape, request efficiency, continuous-collection suitability, and whether page observation should remain primary.

Future War Intelligence Discovery follows the current DQ-EXT-001 / DQ-KEY-002 sequence unless reprioritized by the owner.
